name: oauth2.cocoapods.example
options:
  bundleIdPrefix: com.dracoon.oauth2-example
targets:
  oauth2.example:
    settings:
      ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES: $(inherited)
    type: application
    platform: iOS
    deploymentTarget: "9.3"
    sources:
      - oauth2-example
